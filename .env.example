# Optional: protect admin write API (set same value in admin NEXT_PUBLIC_CATALOG_ADMIN_KEY)
# CATALOG_ADMIN_KEY=

# Optional: extra CORS origins for admin (comma-separated)
# ADMIN_ORIGIN=http://localhost:3000
